<?php
declare(strict_types=1);ini_set('display_errors','stderr');error_reporting(E_ALL);const EXPECTED_DATABASE='u756742628_ilovemybody';if(PHP_SAPI!=='cli')exit(2);
$config=require($argv[1]??'');if(isset($config['database'])&&is_array($config['database']))$config=$config['database'];$database=$config['db']??($config['name']??null);if($database!==EXPECTED_DATABASE)throw new RuntimeException('Unexpected database');$sqlPath=$argv[2]??'';if(!is_file($sqlPath))throw new RuntimeException('Phase 90 SQL missing');
$pdo=new PDO('mysql:host='.$config['host'].';port='.($config['port']??3306).';dbname='.$database.';charset=utf8mb4',$config['user'],$config['pass']??$config['password'],[PDO::ATTR_ERRMODE=>PDO::ERRMODE_EXCEPTION,PDO::MYSQL_ATTR_MULTI_STATEMENTS=>true,PDO::ATTR_DEFAULT_FETCH_MODE=>PDO::FETCH_ASSOC]);$s=$pdo->prepare(file_get_contents($sqlPath));$s->execute();do{if($s->columnCount())$s->fetchAll();}while($s->nextRowset());$s->closeCursor();
$candidates=$pdo->query('SELECT candidate_key,candidate_name,domain,evidence_model FROM ilb_psoriasis_candidate ORDER BY candidate_key')->fetchAll();$counts=$pdo->query('SELECT evidence_status,COUNT(*) n FROM ilb_psoriasis_candidate_state_evidence GROUP BY evidence_status ORDER BY evidence_status')->fetchAll();$withdrawal=(int)$pdo->query('SELECT SUM(withdrawal_tested) FROM ilb_psoriasis_candidate_state_evidence')->fetchColumn();$rechallenge=(int)$pdo->query('SELECT SUM(rechallenge_tested) FROM ilb_psoriasis_candidate_state_evidence')->fetchColumn();$cells=(int)$pdo->query('SELECT COUNT(*) FROM ilb_psoriasis_candidate_state_evidence')->fetchColumn();
if(count($candidates)!==7||$cells!==35||$withdrawal!==0||$rechallenge!==0)throw new RuntimeException('Phase 90 invariant failed');
echo json_encode(['database'=>$database,'phase'=>'P90_CANDIDATE_STATE_MATRIX','candidates'=>$candidates,'candidate_count'=>7,'matrix_cells'=>$cells,'status_counts'=>$counts,'withdrawal_positive_cells'=>$withdrawal,'rechallenge_positive_cells'=>$rechallenge,'disease_modification_proven'=>false,'cure_candidate_identified'=>false,'patient_tables_queried'=>false,'patient_rows_read'=>0,'patient_rows_modified'=>0,'verified'=>true],JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES),PHP_EOL;
